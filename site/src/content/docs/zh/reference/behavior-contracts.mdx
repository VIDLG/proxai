---
title: 行为契约
description: ProxAI 跨版本承诺的稳定行为
---

import { ContractCards, ProxaiCallout } from '@components/ProxaiDocs.jsx';

# 行为契约

这些是 ProxAI 承诺的行为。破坏其中任何一条都是破坏性变更，需要提升主版本号并附迁移说明（见 [C30](#版本演进)）。它们比字段名或配置默认值更稳定。

<ProxaiCallout type="note" title="契约不是教程">
  契约是稳定行为参考。任务页面应链接到这里，而不是完整重复这些承诺。
</ProxaiCallout>

## 配置与启动

<ContractCards layout="single" labels={{ sourceOwner: '源码归属', failureMode: '失败模式', suggestedTests: '建议测试', relatedDocs: '相关文档' }} contracts={[
  { id: 'C1', title: '严格加载配置', promise: '`config.toml` 缺失必填字段一定启动失败。ProxAI 永远不会用隐式默认启动到部分有效的运行时状态。', sourceOwner: '`src/config.rs`', failureMode: 'Proxy 在缺少 provider、route、protocol 或 timeout 数据时仍启动。', suggestedTests: ['配置加载/default 测试', '启动校验测试'], relatedDocs: [{ label: '配置参考', href: '/zh/reference/configuration' }] },
  { id: 'C2', title: '生成本地示例', promise: '首次运行一定在 app 目录生成 `config.example.toml` 作为本地参考。', sourceOwner: '`src/paths.rs` 和配置 bootstrap 代码', failureMode: '新用户无法发现本地配置形态。', suggestedTests: ['Generated app-directory defaults 测试'], relatedDocs: [{ label: '环境与文件', href: '/zh/reference/environment-and-files' }] },
  { id: 'C3', title: '仓库示例是权威示例', promise: '仓库中被跟踪的 `config.example.toml` 是权威示例。ProxAI 运行时从不覆盖它。', sourceOwner: '`config.example.toml` 和配置 bootstrap 代码', failureMode: '运行时生成逻辑修改了仓库文档事实来源。', suggestedTests: ['配置示例生成测试', 'Docs config coverage check'], relatedDocs: [{ label: '配置参考', href: '/zh/reference/configuration' }] },
  { id: 'C4', title: '工具调用超时必须有效', promise: '`[tool_calls].timeout_secs` 必须大于 0，否则 ProxAI 启动失败。', sourceOwner: '`src/config.rs`', failureMode: '工具调用语义卡顿可能无限挂起。', suggestedTests: ['配置校验测试', 'Streaming tool-call timeout 测试'], relatedDocs: [{ label: '默认值与限制', href: '/zh/reference/defaults-and-limits' }] },
]} />

## 监听分离

<ContractCards labels={{ sourceOwner: '源码归属', failureMode: '失败模式', suggestedTests: '建议测试', relatedDocs: '相关文档' }} contracts={[
  { id: 'C5', title: 'MCP 保持本地监听', promise: 'MCP listener 始终绑定本地地址（默认 `127.0.0.1`），ProxAI 自身永远不会把它暴露到外网。', sourceOwner: 'Listener setup 和配置默认值', failureMode: '控制面被暴露到本机之外。', suggestedTests: ['Listener default tests'], relatedDocs: [{ label: '默认值与限制', href: '/zh/reference/defaults-and-limits' }] },
  { id: 'C6', title: 'MCP 不是模型请求通道', promise: 'MCP 只是控制面。模型请求走 Proxy listener，永远不走 MCP。', sourceOwner: 'HTTP routing/listener setup', failureMode: '模型流量进入控制 endpoint。', suggestedTests: ['Route/listener separation tests'], relatedDocs: [{ label: '架构', href: '/zh/developer/architecture' }] },
]} />

## 路由与转换

<ContractCards layout="single" labels={{ sourceOwner: '源码归属', failureMode: '失败模式', suggestedTests: '建议测试', relatedDocs: '相关文档' }} contracts={[
  { id: 'C7', title: '显式路由优先', promise: '只有在没有显式 route 命中时才使用默认 provider。命中的 route 永远优先于默认。', sourceOwner: '`src/routing/`', failureMode: '默认 provider 抢走已命中 route 的流量。', suggestedTests: ['Route matching tests', 'Proxy e2e route tests'], relatedDocs: [{ label: '路由匹配', href: '/zh/reference/route-matching' }] },
  { id: 'C8', title: '不支持的转换对显式失败', promise: '未实现的转换对一定显式失败。当 route 命中但协议对不支持时，ProxAI 永远不会静默 fallthrough 到默认 provider。', sourceOwner: '`src/translation/` 和 routing dispatch', failureMode: '不支持的转换静默路由到其他 provider。', suggestedTests: ['兼容性矩阵覆盖', 'Unsupported pair e2e tests'], relatedDocs: [{ label: '兼容性矩阵', href: '/zh/reference/compatibility-matrix' }] },
  { id: 'C9', title: 'Provider protocol 是语义标识', promise: 'Provider `protocol` 是语义标识。Provider 名字只是用户标签，永远不会改变 wire 行为。', sourceOwner: '`src/config.rs`、`src/provider/`、`src/translation/`', failureMode: 'Provider name 意外改变 serialization 或 response parsing。', suggestedTests: ['Provider protocol dispatch tests'], relatedDocs: [{ label: '术语表', href: '/zh/reference/glossary' }] },
  { id: 'C10', title: 'Request protocol guard 严格生效', promise: '如果 route 设置了 `request_protocol`，并且模型命中但入站协议不同，ProxAI 会返回配置错误，而不是继续路由。', sourceOwner: '`src/routing/`', failureMode: 'Endpoint-specific routes 静默 fall through。', suggestedTests: ['Protocol-aware route matching tests'], relatedDocs: [{ label: '路由匹配', href: '/zh/reference/route-matching' }] },
]} />

## Provider 认证

<ContractCards labels={{ sourceOwner: '源码归属', failureMode: '失败模式', suggestedTests: '建议测试', relatedDocs: '相关文档' }} contracts={[
  { id: 'C11', title: 'OpenAI provider 认证由 ProxAI 控制', promise: 'Provider `api_key` 一定会发给上游。对 OpenAI provider（`openai_responses`、`openai_chat_completions`），以 `Authorization: Bearer <key>` 发送，并忽略客户端传入的任何 `Authorization` header。', sourceOwner: '`src/provider/*/transport`', failureMode: '客户端凭据泄露到上游，或 provider 凭据未使用。', suggestedTests: ['Provider transport header tests'], relatedDocs: [{ label: 'Provider 设置', href: '/zh/using/provider-setup' }] },
  { id: 'C12', title: 'Anthropic provider 使用 x-api-key', promise: '对 Anthropic Messages provider，key 以 `x-api-key` 发送。', sourceOwner: '`src/provider/*/transport`', failureMode: 'Anthropic-compatible 上游拒绝认证或收到错误 header。', suggestedTests: ['Anthropic transport header tests'], relatedDocs: [{ label: 'Provider 设置', href: '/zh/using/provider-setup' }] },
]} />

## 响应重建

<ContractCards labels={{ sourceOwner: '源码归属', failureMode: '失败模式', suggestedTests: '建议测试', relatedDocs: '相关文档' }} contracts={[
  { id: 'C13', title: 'Content-Type 由出站协议决定', promise: '响应 `Content-Type` 由 outbound 协议决定，不透传上游的。', sourceOwner: '`src/http_support/` 和 response translation', failureMode: '客户端收到与入站协议不兼容的上游 content type。', suggestedTests: ['Response reconstruction tests'], relatedDocs: [{ label: '协议', href: '/zh/reference/protocols' }] },
  { id: 'C14', title: 'Header 会被过滤', promise: 'Upstream headers 在转发前会经过过滤。可转发的集合由 ProxAI 显式控制，永远不会原样复制全部上游 headers。', sourceOwner: '`src/http_support/`', failureMode: '私有或协议不兼容的上游 headers 泄露给客户端。', suggestedTests: ['Header filtering tests'], relatedDocs: [{ label: '错误响应', href: '/zh/reference/error-responses' }] },
]} />

## 错误

<ContractCards layout="single" labels={{ sourceOwner: '源码归属', failureMode: '失败模式', suggestedTests: '建议测试', relatedDocs: '相关文档' }} contracts={[
  { id: 'C15', title: '保留真实上游诊断 header', promise: '对非 2xx 上游响应，ProxAI 在上游确实提供时保留有用的诊断 headers：`Retry-After`、上游 request id、rate-limit headers。永远不会伪造这些 headers。', sourceOwner: '`src/error/` 和 upstream response handling', failureMode: '客户端丢失可操作 retry/rate-limit 信息，或收到伪造数据。', suggestedTests: ['Upstream error projection tests'], relatedDocs: [{ label: '错误响应', href: '/zh/reference/error-responses' }] },
  { id: 'C16', title: '客户端错误 payload 包含 status', promise: '面向客户端的错误一定在 payload 内包含数字 `status` 字段。这对 SSE error event 尤其重要，因为流式开始后无法再修改 HTTP status。', sourceOwner: '`src/error/render.rs`', failureMode: 'SSE error events 丢失 status 上下文。', suggestedTests: ['HTTP and SSE error rendering tests'], relatedDocs: [{ label: '错误响应', href: '/zh/reference/error-responses' }] },
  { id: 'C17', title: '内部错误分类不外泄', promise: 'ProxAI 永远不会原样暴露内部强类型 error taxonomy。面向客户端的 `type` 取值是固定的稳定枚举，且仅以加法扩展。', sourceOwner: '`src/error/`', failureMode: '内部 Rust error 名称变成 public API。', suggestedTests: ['Error type projection tests', 'Docs checker error type coverage'], relatedDocs: [{ label: '错误处理内部实现', href: '/zh/developer/error-handling-internals' }] },
  { id: 'C18', title: '不伪造上游 code 和 param', promise: 'ProxAI 永远不会伪造上游 `code` 或 `param` 取值。只有上游确实提供时才保留。', sourceOwner: '`src/error/`', failureMode: '客户端基于伪造上游 metadata 做决策。', suggestedTests: ['Upstream error body shape tests'], relatedDocs: [{ label: '错误响应', href: '/zh/reference/error-responses' }] },
]} />

## 流式

<ContractCards layout="single" labels={{ sourceOwner: '源码归属', failureMode: '失败模式', suggestedTests: '建议测试', relatedDocs: '相关文档' }} contracts={[
  { id: 'C19', title: '流必须观测终止事件', promise: 'SSE 流一定被检测 terminal event。只有收到预期 terminal event（`response.completed`、`[DONE]` 或 `message_stop`）才认为流完整。', sourceOwner: 'SSE scanner 和 streaming translation modules', failureMode: '客户端把语义不完整的流当作成功。', suggestedTests: ['Terminal event tests', 'Incomplete stream tests'], relatedDocs: [{ label: '流式行为', href: '/zh/protocol/streaming-behavior' }] },
  { id: 'C20', title: '工具调用卡顿可预期关闭', promise: '卡住的 tool-call argument 流一定在 `[tool_calls].timeout_secs` 内关闭。ProxAI 永远不会让客户端在工具调用参数开始后无限挂起。', sourceOwner: 'Streaming state machines 和 tool-call timeout handling', failureMode: '客户端在部分工具参数之后永远挂起。', suggestedTests: ['Tool-call stall tests'], relatedDocs: [{ label: '流式与工具调用', href: '/zh/using/streaming-and-tools' }] },
  { id: 'C21', title: 'SSE 载体被保留', promise: 'SSE bytes 和 `text/event-stream` content type 在翻译过程中被保留。', sourceOwner: '`src/http_support/`、SSE translation modules', failureMode: 'Streaming 客户端收到非 SSE carrier 数据。', suggestedTests: ['SSE carrier tests'], relatedDocs: [{ label: '流式内部实现', href: '/zh/developer/streaming-internals' }] },
  { id: 'C22', title: '读取超时是 idle-based', promise: '`read_idle_timeout_secs` 是 idle-read 超时，不是整条请求的总时长上限。它只在配置窗口内没有新上游 bytes 到达时触发。', sourceOwner: 'Provider transport/upstream read handling', failureMode: '长但活跃的 stream 被错误截断。', suggestedTests: ['Idle timeout tests'], relatedDocs: [{ label: '默认值与限制', href: '/zh/reference/defaults-and-limits' }] },
]} />

## 隐私

<ContractCards labels={{ sourceOwner: '源码归属', failureMode: '失败模式', suggestedTests: '建议测试', relatedDocs: '相关文档' }} contracts={[
  { id: 'C23', title: '不日志私有请求数据', promise: 'ProxAI 永远不日志请求 body、`Authorization` header、API key 或私有 prompt。', sourceOwner: '`src/observe/` 和 logging call sites', failureMode: '私有 prompts 或凭据出现在默认日志中。', suggestedTests: ['Logging redaction review', 'Request hint tests'], relatedDocs: [{ label: '隐私与本地数据', href: '/zh/using/privacy-and-local-data' }] },
  { id: 'C24', title: 'Capture 保持本地', promise: 'Capture artifact 仅是 app 目录下的本地文件，永远不会被 ProxAI 自身传输。', sourceOwner: '`src/observe/` capture writers', failureMode: 'Capture artifacts 自动离开本机。', suggestedTests: ['Capture writer tests'], relatedDocs: [{ label: '环境与文件', href: '/zh/reference/environment-and-files' }] },
  { id: 'C25', title: 'Capture 路径固定', promise: 'Capture 路径固定，不可配置，避免误写到任意位置。', sourceOwner: '`src/observe/` 和 paths code', failureMode: '用户提供路径把私有 captures 写到不安全位置。', suggestedTests: ['Capture path tests'], relatedDocs: [{ label: 'Capture phases', href: '/zh/reference/capture-phases' }] },
]} />

## 转换纯度

<ContractCards layout="single" labels={{ sourceOwner: '源码归属', failureMode: '失败模式', suggestedTests: '建议测试', relatedDocs: '相关文档' }} contracts={[
  { id: 'C26', title: '转换在 carrier 边界保持纯粹', promise: '转换在 HTTP 载体边界保持纯函数。它接收 protocol 值和 payload/stream carrier，不接收 HTTP `Response`/`Body`、路由细节、模型重写细节或 provider 私有结构。', sourceOwner: '`src/translation/`', failureMode: 'Translation 依赖 HTTP/provider internals，无法按 pair 推理。', suggestedTests: ['Boundary-focused conversion tests'], relatedDocs: [{ label: '模块边界', href: '/zh/developer/architecture/module-boundaries' }] },
  { id: 'C27', title: '不原样 round-trip 上游结构', promise: 'ProxAI 永远不在转换 pair 之间原样 round-trip 上游结构。每个方向独立翻译成目标协议的类型。', sourceOwner: '`src/translation/`', failureMode: '目标协议收到 provider-private structures。', suggestedTests: ['Pair-specific response conversion tests'], relatedDocs: [{ label: '协议转换', href: '/zh/developer/protocol-conversion' }] },
  { id: 'C28', title: 'Provider 怪癖留在 provider 代码', promise: 'Provider-local normalization 属于 `provider/`。通用跨协议 shape 改写属于 `translation/`。Provider 代码不得隐藏协议到协议的转换。', sourceOwner: '`src/provider/` 和 `src/translation/`', failureMode: '协议转换逻辑隐藏在 provider-specific adapters 中。', suggestedTests: ['Provider/request and translation boundary review'], relatedDocs: [{ label: '该改哪里', href: '/zh/developer/where-to-change-code' }] },
]} />

## 版本演进

<ContractCards labels={{ sourceOwner: '源码归属', failureMode: '失败模式', suggestedTests: '建议测试', relatedDocs: '相关文档' }} contracts={[
  { id: 'C29', title: '客户端错误 type 只加法扩展', promise: '面向客户端的 `type` 取值仅以加法扩展。已有取值永远不会被重命名或删除，除非主版本号升级。', sourceOwner: '`src/error/render.rs` 和 reference docs', failureMode: '既有客户端因错误 type 重命名或删除而破坏。', suggestedTests: ['Error type coverage checker'], relatedDocs: [{ label: '错误响应', href: '/zh/reference/error-responses' }] },
  { id: 'C30', title: '破坏契约需要主版本升级', promise: '破坏本页任何契约（C1–C30）都是破坏性变更，需要主版本号升级并附迁移说明。', sourceOwner: 'Release process 和 docs', failureMode: '用户可见破坏行为以 minor/patch 发布。', suggestedTests: ['Release checklist review'], relatedDocs: [{ label: '贡献者检查清单', href: '/zh/developer/contributor-checklist' }] },
]} />

## 相关参考

- [配置参考](/zh/reference/configuration)
- [默认值与限制](/zh/reference/defaults-and-limits)
- [错误响应](/zh/reference/error-responses)
- [协议](/zh/reference/protocols)
