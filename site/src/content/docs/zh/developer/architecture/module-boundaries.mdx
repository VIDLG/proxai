---
title: 模块边界
description: 解析、路由、转换、provider 行为、HTTP carrier 与观测分别放在哪里的维护者指南
robots: noindex
---

import { ModuleGrid, ProxaiCallout, ReferenceTable } from '@components/ProxaiDocs.jsx';

# 模块边界

本页是维护者判断代码归属的地图。拿不准时，保持协议转换显式，并避免把 provider-specific 行为混入通用 translation。

<ProxaiCallout type="caution" title="不要混淆协议和 provider 边界">
  Provider 名称是用户标签。协议名称描述 wire 行为。Provider 模块可以适配本地 provider 怪癖，但通用协议到协议的 shape 改写应放在 `crates/proxai-core/src/translation/`。
</ProxaiCallout>

## 归属地图

<ModuleGrid
  modules={[
    { name: 'src/pipeline/inbound.rs', icon: 'route', description: '从 HTTP path 检测入站协议，解析 JSON bytes，映射应用错误并接入 request-scoped observer。' },
    { name: 'crates/proxai-core/src/ingress/', icon: 'route', description: '在 routing 和 translation 前归一化并校验结构化入站 payload。' },
    { name: 'crates/proxai-core/src/observe.rs', icon: 'gauge', description: '共享 observer contract、封闭的 core observation variants 和 no-op 默认实现；不包含具体 sink。' },
    { name: 'crates/proxai-core/src/protocol/', icon: 'protocol', description: 'Wire structs、enums 和协议专属 event payload。不做 provider routing 决策。' },
    { name: 'crates/proxai-core/src/routing/', icon: 'route', description: '编译模型规则，按 request protocol/model 选择 provider 标签，改写 upstream model，并执行严格协议 guard。' },
    { name: 'crates/proxai-core/src/translation/', icon: 'flow', description: '协议 pair 之间的纯 request/response/stream 转换。' },
    { name: 'crates/proxai-core/src/provider/', icon: 'file', description: '与 carrier 无关的 provider compatibility policy 和结构化响应归一化。' },
    { name: 'src/provider/*/request', icon: 'file', description: '应用层 Provider request preparation：模型改写、provider projection、序列化。' },
    { name: 'src/provider/*/transport', icon: 'terminal', description: '上游 URL 构造、provider-owned auth headers、HTTP send。' },
    { name: 'src/upstream/', icon: 'timer', description: '读取 provider status、headers、非流式 body 和流式 byte carrier。' },
    { name: 'src/http_support/', icon: 'architecture', description: 'HTTP response reconstruction、content-type helpers、header filtering、byte stream carriers。' },
    { name: 'src/error/', icon: 'alert', description: 'Domain error taxonomy 和紧凑的客户端错误投影。' },
    { name: 'src/observe/', icon: 'gauge', description: '结构化日志、capture phases、hints 和隐私安全诊断。' },
  ]}
/>

## 边界判断

<ReferenceTable
  columns={['问题', '放在']}
  rows={[
    ['是否解析客户端请求 path 或 body bytes？', '`src/pipeline/inbound.rs`'],
    ['是否归一化或校验结构化入站 payload？', '`crates/proxai-core/src/ingress/`'],
    ['是否定义可复用的 core observation contract 或 event？', '`crates/proxai-core/src/observe.rs`'],
    ['是否定义协议 JSON wire shape？', '`crates/proxai-core/src/protocol/`'],
    ['是否按协议/模型选择 provider 标签？', '`crates/proxai-core/src/routing/`'],
    ['是否把选中的 provider 标签映射到 HTTP transport？', '`src/pipeline/provider_request.rs`'],
    ['是否把一种协议 payload 转成另一种协议 payload？', '`crates/proxai-core/src/translation/`'],
    ['是否修复已测量的 provider 结构化响应偏差？', '`crates/proxai-core/src/provider/`'],
    ['是否改写 provider model 或 provider-local request 字段？', '`src/provider/*/request`'],
    ['是否添加 provider 认证头或构造上游 URL？', '`src/provider/*/transport`'],
    ['是否过滤 headers 或重建 HTTP response？', '`src/http_support/`'],
    ['是否渲染客户端错误？', '`src/error/`'],
    ['是否写日志或 capture artifacts？', '`src/observe/`'],
  ]}
  codeFirstColumn={false}
/>

## Observation 与日志边界

`proxai-core` 定义共享 `Observer` contract、封闭的 `Observation` variants 和 no-op 默认实现。Core ingress、provider normalization 与 translation 通过该 contract 发出类型化 variants。它们不选择 `tracing`、诊断文件、capture 存储、metrics backend 或其他具体 sink；具体实现由下游应用组合层提供。

对于 request-scoped 代码，稳定的生命周期和领域事件应通过 `ObserveContext` 发出。`ObserveSinks` 再决定一个 point 应进入日志、diagnostics、capture，还是同时进入多个 sink。只有 logging sink 负责把 observation 转成 `tracing` event，并选择日志级别和输出格式。

<ReferenceTable
  columns={['场景', '使用方式']}
  rows={[
    ['Core 中合法但有损的适配，或 ingress compatibility event', '通过共享 `Observer` 发出类型化 `Observation` variant；具体 sink 由下游决定。'],
    ['请求生命周期、协议/provider 结果或失败现场', '使用带类型 point 的 `ObserveContext`。'],
    ['Logging sink 实现', '直接使用 `tracing`；日志级别和格式在这里决定。'],
    ['Observation/capture 子系统自身失败', '直接使用 `tracing`，避免 observer 递归观测自身。'],
    ['没有 request context 的进程启动、配置或后台任务', '直接使用 `tracing`。'],
    ['临时局部实现调试', '使用 `tracing::trace!`；若反复用于真实诊断，则提升为 observe point。'],
  ]}
  codeFirstColumn={false}
/>

## 反模式

<ReferenceTable
  columns={['避免', '原因']}
  rows={[
    ['把 HTTP `Response` 传进 `crates/proxai-core/src/translation/`', 'Translation 应在 carrier 边界保持纯粹。'],
    ['在 translation 中添加 provider auth 逻辑', 'Auth 是 provider transport 行为，不是协议转换。'],
    ['把 provider 名称当协议值使用', 'Provider 名称是任意用户标签。'],
    ['协议 guard mismatch 后静默 fallback', '匹配 route 但 `request_protocol` 不一致是配置错误。'],
    ['为了方便记录 request body', 'Prompt、工具参数和输出都是私有数据。'],
  ]}
  codeFirstColumn={false}
/>

## 相关文档

- [请求生命周期](/zh/developer/architecture/request-lifecycle)
- [协议转换](/zh/developer/protocol-conversion)
- [行为契约](/zh/reference/behavior-contracts)
- [捕获阶段](/zh/reference/capture-phases)
