---
title: Configuration Example
description: Full reference of the generated config.example.toml
---

import { Code } from '@astrojs/starlight/components';
import { ProxaiCallout } from '@components/ProxaiDocs.jsx';
import configExample from '../../../../../../config.example.toml?raw';

# Configuration Example

This is the complete reference for the tracked `config.example.toml`. ProxAI writes this file into the app directory on first run, then loads your local `config.toml` for actual runtime settings.

<ProxaiCallout type="caution" title="Never commit secrets">
  Treat `config.example.toml` as a template. Put real `api_key` values only in your local git-ignored `config.toml`.
</ProxaiCallout>

<Code code={configExample} lang="toml" title="config.example.toml" />

For field-by-field explanations, see [Configuration](/en/configuration).
