---
title: Anthropic Messages
description: Anthropic Messages protocol model in ProxAI
---

import { ProtocolQuickFacts } from '@components/ProxaiDocs.jsx';

# Anthropic Messages

ProxAI uses the protocol name `anthropic_messages` for the Anthropic Messages API.

<ProtocolQuickFacts
  protocol="anthropic_messages"
  requestPath="/v1/messages"
  codeAreas={[
    'crates/proxai-core/src/protocol/anthropic/messages/wire',
    'src/provider/anthropic_messages',
    'crates/proxai-core/src/ingress/anthropic_messages',
    'crates/proxai-core/src/translation/anthropic_messages',
  ]}
  convertsTo={['openai_responses']}
/>

## Response shape

A non-streaming Anthropic Messages response is a single top-level `Message` envelope:

```json
{
  "id": "msg_...",
  "type": "message",
  "role": "assistant",
  "model": "...",
  "content": [
    { "type": "text", "text": "..." }
  ],
  "stop_reason": "end_turn",
  "usage": {}
}
```

Multiple output pieces live in `content: Vec<ContentBlock>`, not in multiple top-level messages.

## Stop reason

`stop_reason` explains why generation stopped:

- `end_turn` — natural end of the assistant turn
- `tool_use` — model requested tool execution
- `max_tokens` — output was truncated by token limit
- `stop_sequence` — configured stop sequence matched
- `pause_turn` — turn paused
- `refusal` — model refused

Wire structs mirror official presence and nullability: required-nullable fields use `RequiredNullable<T>` and reject missing fields. Narrow provider normalization may insert explicit `null` before protocol deserialization for a measured compatibility deviation; it does not loosen the wire carrier.

## Streaming

Streaming is event-based: `message_start`, content block events, `message_delta`, and `message_stop`. The final stop reason is usually carried by `message_delta.delta.stop_reason`.

For cross-protocol streaming details, see [Protocol Conversion](/developer/protocol-conversion).
